<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script type="text/javascript" defer src="/js/script.js"></script>
    <title>{{ identity.title }}</title>
</head>
<body class="bg-gray-100 font-sans leading-normal tracking-normal">


<nav class="bg-white p-6 fixed w-full z-10 top-0">
    <div class="container mx-auto flex justify-between items-center">

        <div class="flex items-start">
            <img src="/assets/logognc.png" alt="Logo" class="h-10 mr-2">
            <a class="font-bold text-xl" href="#">{{ identity.title }}</a>
        </div>

        <div class="flex justify-center items-center flex-grow">
            <a class="mr-4" href="#">All</a>
            <a class="mr-4" href="#">Teams</a>
        </div>

        <div class="flex items-end">
            <form action="/seacrh" class="search__form" role="search" method="post">
                <input type="search" id="searchInput" name="searchTearm" aria-label="Search" placeholder="Search..." class="border border-gray-300 px-3 py-2 rounded-lg mr-4">
            <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Search</button>
            </form>
        </div>
        <div class="flex overflow-hidden bg-white border ml-6 divide-x rounded-lg rtl:flex-row-reverse dark:bg-gray-900 dark:border-gray-700 dark:divide-gray-700">
            <a href="/upload">
                <button class="flex items-center px-4 py-2 text-sm font-medium text-gray-600 transition-colors duration-200 sm:text-base sm:px-6 dark:hover:bg-gray-800 dark:text-gray-300 gap-x-3 hover:bg-gray-100" >
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 sm:w-6 sm:h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 16.5V9.75m0 0l3 3m-3-3l-3 3M6.75 19.5a4.5 4.5 0 01-1.41-8.775 5.25 5.25 0 0110.233-2.33 3 3 0 013.758 3.848A3.752 3.752 0 0118 19.5H6.75z" />
                    </svg>
                    <span>Upload</span>
                </button>
            </a>
        </div>
    </div>
</nav>

<header class="bg-cover bg-center h-screen relative flex items-center justify-center" style="background-image: linear-gradient(rgba(0, 0, 0, 0.75), rgba(0, 0, 0, 0.75)), url('/assets/gncsiswa.jpeg');">
    <div class="container mx-auto text-center text-white">
        <h1 class="text-4xl lg:text-6xl font-bold leading-tight mb-8">Welcome To {{ identity.title }}</h1>
        <p class="text-lg lg:text-xl">{{ identity.description }}</p>
    </div>
</header>

<section class="container px-6 py-12">
    <h2 class="text-3xl font-bold mb-6">Berita Terbaru</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        
        {{#each berita}}
        
        <a href="/berita/{{this.no}}">
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
            <img class="w-full h-48 object-cover object-center" src="./assets/berita/{{this.gambar}}" alt="Berita {{this.no}}">
            <div class="p-6">
                <h3 class="text-xl font-bold mb-2">{{this.judul_berita}}</h3>
                <p class="text-gray-700 mb-2">{{this.isi}}</p>
                <span class="text-sm text-gray-500">{{this.date}}</span>
            </div>
        </div>
        </a>
        {{/each}}
    </div>
</section>





    <footer class="bg-gray-900 text-white py-6">
        <div class="container mx-auto text-center">
            <p>{{ identity.title }}</p>
        </div>
    </footer>

</body>
</html>
